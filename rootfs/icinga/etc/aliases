# See man 5 aliases for format
postmaster:    root

# TICKET ID
icinga-rt:      |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'General'
            --action correspond
            --url http://localhost"

icinga-xrt:     |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'General'
            --action comment
            --url http://localhost"

icinga-events:  |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Events'
            --action correspond
            --url http://localhost"

icinga-partner: |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Partner'
            --action correspond
            --url http://localhost"

icinga-shop:    |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Shop'
            --action correspond
            --url http://localhost"

icinga-support: |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Support'
            --action correspond
            --url http://localhost"

icinga-sales: |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Sales'
            --action correspond
            --url http://localhost"

icinga-automail: |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Automail'
            --action correspond
            --url http://localhost"

icinga-infrastructure: |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Infrastructure'
            --action correspond
            --url http://localhost"

icinga-privacy: |"/opt/rt4/bin/rt-mailgate
            --extension ticket
            --queue 'Infrastructure'
            --action correspond
            --url http://localhost"
